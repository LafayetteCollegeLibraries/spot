<%=
  f.input :publisher,
          as: :multi_value,
          input_html: {
            class: 'form-control',
            data: {
              'autocomplete-url' => '/authorities/search/local/publisher',
              'autocomplete' => 'publisher'
            },
          },
          required: f.object.required?(:publisher)
%>
