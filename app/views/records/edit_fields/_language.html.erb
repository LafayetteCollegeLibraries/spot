<%=
  f.input key,
  as: :controlled_vocabulary,
  input_html: {
    class: 'form-control ui-autocomplete-input',
    data: {
      'autocomplete-url' => '/authorities/search/languages',
      'autocomplete' => key,
    },
  },
  wrapper_html: {
    data: {
      'autocomplete-url' => '/authorities/search/languages',
      'field-name' => key
    }
  },
  required: f.object.required?(key)
%>
