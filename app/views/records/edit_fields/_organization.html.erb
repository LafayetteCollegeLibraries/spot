<%=
  f.input :organization,
          as: :multi_value,
          input_html: {
            class: 'form-control',
            data: {
              'autocomplete-url' => '/authorities/search/local/organization',
              'autocomplete' => 'organization'
            },
          },
          required: f.object.required?(:organization)
%>
