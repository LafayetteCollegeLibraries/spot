<%=
  f.input :keyword,
          as: :multi_value,
          input_html: {
            class: 'form-control',
            data: {
              'autocomplete-url' => '/authorities/search/local/keyword',
              'autocomplete' => 'keyword'
            },
          },
          required: f.object.required?(:keyword)
%>
