<%=
  f.input :source,
          as: :multi_value,
          input_html: {
            class: 'form-control',
            data: {
              'autocomplete-url' => '/authorities/search/local/source',
              'autocomplete' => 'source'
            },
          },
          required: f.object.required?(:source)
%>
