<%=
  f.input :editor,
          as: :multi_value,
          input_html: {
            class: 'form-control',
            data: {
              'autocomplete-url' => '/authorities/search/local/name',
              'autocomplete' => 'editor'
            },
          },
          required: f.object.required?(:editor)
%>
